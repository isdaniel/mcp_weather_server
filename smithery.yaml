runtime: python

startCommand:
  type: http
  command: python
  args:
    - "-m"
    - "mcp_weather_server"
    - "--mode"
    - "streamable-http"
    - "--host"
    - "0.0.0.0"
    - "--port"
    - "8081"
