runtime: python

startCommand:
  type: sse
  configSchema:
    type: object
  commandFunction: |
    config => ({ command: "uv", args: ["run", "mcp_weather_server", "--mode", "sse", "--host", "localhost", "--port", "3000"] })
