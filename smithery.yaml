runtime: container

startCommand:
  type: http
  configSchema:
    type: object
    properties:
      debug:
        type: boolean
        title: "Debug Mode"
        description: "Enable debug logging"
        default: false

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
